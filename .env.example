# API key de bcvapi.tech (regístrate gratis en https://www.bcvapi.tech/).
# Sin prefijo VITE_ → solo accesible server-side, NO termina en el bundle del cliente.
BCV_API_KEY=tu_api_key_aqui

# URL pública del endpoint (relativa para evitar CORS).
VITE_BCV_API_URL=/api/bcv
